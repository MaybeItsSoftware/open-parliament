# Copy this file to `assets/env/.env` before running the app locally
# (`assets/env/.env` is gitignored). This file is entirely optional — running
# without it is fine too; Sentry just stays disabled, same as leaving
# SENTRY_DSN blank below.
SENTRY_DSN=
